[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "style-lint"
version = "0.2.1"
description = "Shared code style linting rules"
dependencies = ["pytest>=7.0"]
